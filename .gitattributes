# The wire-format vectors are hashed byte for byte by the test suites in this
# repository and in the agent, and the two copies must be identical. Line
# ending conversion would change that hash on checkout and break both, so this
# file is exempt from all normalisation.
tests/wire_vectors.json -text
